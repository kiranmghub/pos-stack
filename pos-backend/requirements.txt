pytz
Django==4.2.23
djangorestframework==3.15.2
djangorestframework-simplejwt==5.3.1
django-filter==24.3
drf-spectacular==0.27.2
channels==4.1.0
channels-redis==4.2.0
celery==5.4.0
redis==5.0.7
psycopg2-binary==2.9.9   # if Postgres
python-dotenv==1.0.1
requests==2.32.5
urllib3<2.0  # Pin to v1.x for LibreSSL compatibility on macOS
Pillow>=10.0.0  # Image processing for document uploads
reportlab>=4.0.0  # PDF generation for image-to-PDF conversion
openpyxl>=3.1.0  # Excel export for reports
pdfplumber>=0.10.0  # PDF text extraction
python-doctr>=1.0.0  # Deep learning OCR for scanned PDFs (primary)
torch>=2.0.0,<3.0.0  # PyTorch backend for docTR (required for docTR)
torchvision>=0.15.0  # PyTorch vision utilities (required by docTR)
numpy<2.0.0  # Pin to NumPy 1.x for PyTorch compatibility (PyTorch 2.2.2 requires NumPy <2.0)
opencv-python<4.9.0  # Pin to version compatible with NumPy 1.x (opencv-python 4.9+ requires NumPy 2+)
pytesseract>=0.3.10  # OCR for scanned PDFs (fallback)
pdf2image>=1.16.0  # PDF to image conversion for OCR (fallback)
